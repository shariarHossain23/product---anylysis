import React from 'react';

const HomeReview = ({person}) => {
    const {name,rating,review} = person;
    console.log(person);
    return (
        <div>
            <h1></h1>
        </div>
    );
};

export default HomeReview;